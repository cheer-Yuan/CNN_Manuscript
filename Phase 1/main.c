#include <stdio.h>
#include "reader.h"
#include <stdlib.h>

int main(int argc, char **argv)
{

    //image *image_P = readppm_File("hsf_0_00000.ppm");
    //printf("%d, %d, %d\n", image_P->data[0].red, image_P->data[0].green, image_P->data[0].blue);

}

/*
main{

　　　　 readData()；
        init(){}
        train(){
    do{
        for（i < Data）{

            compute output(i)；
            adderror；
            backupdate(i)；
        }
    }while（i < train or converge）；
}

write()；
test, retrain；
return 0；
}
*/